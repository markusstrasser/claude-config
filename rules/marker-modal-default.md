# Marker PDF→Markdown — Use Modal, Not Local

PDF → markdown extraction with `marker-pdf`: default to Modal-hosted Marker (`corpus ingest --pdf …`, T4, ~1 min, ~$0.01), NOT local marker_single — local MPS crashes (surya bug), CPU is ~20× slower.
Details, direct-invoke pattern, deploy check, and local-is-fine exceptions: `~/Projects/skills/modal/references/marker-pdf-extraction.md`.
